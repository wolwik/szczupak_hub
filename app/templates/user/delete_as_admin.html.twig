{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.user_delete'|trans }}
{% endblock %}

{% block body %}
    <h1>
        {{ 'title.user_delete'|trans }}
    </h1>

    <p>
        {{ 'message.delete_confirm'|trans }}
    </p>

    {{ form_start(form) }}

    <div class="form-group">
        {{ form_widget(form.delete) }}
    </div>

    {{ form_end(form) }}

    <a href="{{ path('user_list') }}" class="btn btn-sm btn-danger">
        {{ 'action.back_to_list'|trans }}
    </a>
{% endblock %}
